name: Deploy to GKE using Terraform

on:
  push:
    branches:
      - main

jobs:
  terraform:
    name: Terraform Deployment
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Authenticate with Google Cloud
        uses: google-github-actions/auth@v1
        with:
          credentials_json: ${{ secrets.GCP_CREDENTIALS }}

      - name: Setup Google Cloud SDK and Authentication
        run: |
          echo '${{ secrets.GCP_CREDENTIALS }}' > $HOME/gcloud.json
          chmod 600 $HOME/gcloud.json
          echo "GOOGLE_APPLICATION_CREDENTIALS=$HOME/gcloud.json" >> $GITHUB_ENV
          ls -la $HOME  # Debugging step to check if the file exists

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: 1.6.6

      - name: Initialize Terraform
        run: terraform init
        working-directory: GKE
        env:
          GOOGLE_APPLICATION_CREDENTIALS: $HOME/gcloud.json

      - name: Terraform Plan
        run: terraform plan -out=tfplan
        working-directory: GKE
        env:
          GOOGLE_APPLICATION_CREDENTIALS: $HOME/gcloud.json
          GOOGLE_PROJECT: ${{ secrets.GCP_PROJECT_ID }}
          GOOGLE_REGION: ${{ secrets.GCP_REGION }}

      - name: Terraform Apply
        run: terraform apply -auto-approve tfplan
        working-directory: GKE
        env:
          GOOGLE_APPLICATION_CREDENTIALS: $HOME/gcloud.json
          GOOGLE_PROJECT: ${{ secrets.GCP_PROJECT_ID }}
          GOOGLE_REGION: ${{ secrets.GCP_REGION }}

